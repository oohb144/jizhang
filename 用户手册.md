# 记账本 用户使用手册

## 一、应用简介

记账本是一款简洁实用的个人财务记账应用，帮助你记录每日收支、管理多账户余额、设置分类预算并追踪消费进度。

**核心特性：**
- 多账户管理（微信、支付宝、银行卡等）
- 支出/收入分类记账
- 自定义分类体系，自由添加主分类和子分类
- 智能预算自动汇总（子分类 → 主分类 → 总预算）
- 可视化图表分析（柱状图、饼图）
- 所有数据本地存储，安全隐私

---

## 二、首页概览

打开应用进入首页，可以看到以下内容：

| 区域 | 说明 |
|------|------|
| 总资产卡片 | 显示所有账户的余额总和，点击"设置余额"可跳转 |
| 账户卡片 | 横向滑动查看各账户余额（微信、支付宝等） |
| 今日预算 | 当日预算额度、已用金额、剩余金额及进度条 |
| 本月概览 | 本月总支出、总收入、预算剩余 |
| 分类预算进度 | 已分配预算的分类进度详情（可展开子分类） |
| 快捷操作 | 账户设置、预算管理、历史记录、交易记录 |
| 最近交易 | 最近 10 笔交易记录 |

下拉首页即可刷新数据。

---

## 三、记账

### 3.1 记录一笔支出

1. 点击首页右下角 **"记账"** 按钮
2. 确认顶部开关处于 **"支出"**（红色）状态
3. 输入金额（最多两位小数）
4. 选择付款账户（微信/支付宝等）
5. 选择分类：
   - 先点 **一级分类**（如"餐饮"）
   - 再点 **二级分类**（如"午餐"）
   - 只显示已设置预算的分类
6. （可选）输入备注
7. 点击 **"确认记账"**

### 3.2 记录一笔收入

1. 点击首页右下角 **"记账"** 按钮
2. 将顶部开关切换到 **"收入"**（绿色）
3. 输入金额
4. 选择收款账户
5. （可选）输入备注
6. 点击 **"确认记账"**

> 注：收入记账无需选择分类，已简化操作流程。

### 3.3 注意事项

- **支出分类**：只显示已设置预算的分类。若提示"请先在预算管理中为分类设置预算"，需先前往预算管理页面设置
- **收入**：无需选择分类，只需选择账户

---

## 四、账户管理

### 4.1 设置账户余额

1. 点击首页右上角的 **钱包图标**，或点击快捷操作区的 **"账户设置"**
2. 输入每个账户的 **实际余额**（覆盖式写入，非累加）
3. 点击 **"保存余额"**

### 4.2 添加新账户

1. 进入"账户设置"页面
2. 点击 **"添加新账户"**
3. 输入账户名称（如"银行卡"、"现金"）和初始余额
4. 点击 **"添加"**

---

## 五、预算管理

### 5.1 预算层级结构

本应用采用三级自动汇总预算体系：

```
月度总预算（自动汇总 = 所有主分类之和）
├── 餐饮（自动汇总 = 子分类之和）
│   ├── 早餐 [← 在此输入预算]
│   ├── 午餐 [← 在此输入预算]
│   ├── 晚餐 [← 在此输入预算]
│   └── 零食/夜宵 [← 在此输入预算]
├── 交通（自动汇总 = 子分类之和）
│   ├── 公交/地铁 [← 在此输入预算]
│   ├── 打车 [← 在此输入预算]
│   └── 加油/停车 [← 在此输入预算]
└── ...（其他分类）
```

**核心规则：**
- **月度总预算** = 所有主分类预算之和，自动计算，不可手动输入
- **主分类预算** = 该分类下所有子分类预算之和，自动计算，不可手动输入
- **只有子分类有输入框**，用户只需在子分类处设置预算

### 5.2 设置预算

1. 点击首页快捷操作区的 **"预算管理"** 或首页右上角的 **饼图图标** 进入
2. 在子分类的输入框中填写预算金额（如早餐 15、午餐 20、晚餐 25）
3. 主分类"餐饮"会自动显示预算 60
4. 所有主分类预算汇总后，顶部"月度总预算"自动显示总额
5. 点击 **"保存预算"** 完成设置

### 5.3 添加已有分类预算

1. 在预算管理页面点击 **"添加已有分类"** 按钮
2. 在弹出的对话框中选择要添加预算的子分类
3. 添加后在输入框中填写金额

### 5.4 添加自定义分类

1. 点击预算管理页面右上角的 **+** 按钮
2. 在弹出的对话框中：
   - 输入主分类名称（如"旅行"、"宠物"）
   - 选择图标（16 种常用图标可选）
   - 输入子分类数量
   - 逐个填写每个子分类的名称
3. 点击 **"添加"**，自定义分类将添加到分类列表中
4. 在子分类输入框中填写预算金额并保存

### 5.5 查看预算进度

1. 首页的 **"分类预算进度"** 卡片展示所有已分配预算的分类
2. 每个主分类卡片显示：
   - 总预算 / 总支出 / 剩余金额
   - 整体进度条
3. 点击展开主分类可查看各子分类的详细预算进度

---

## 六、历史记录

点击首页快捷操作区的 **"历史记录"** 进入。

### 6.1 年月选择器

- 通过下拉菜单选择要查看的年份和月份
- 默认显示当前月份

### 6.2 统计摘要卡片

- 总支出（红色）、总收入（绿色）
- 预算总额与预算剩余
- 预算进度条，超支时显示红色警告

### 6.3 年度收支趋势图（柱状图）

- 红色柱：每月总支出
- 绿色柱：每月总收入
- 展示当年各月的收支对比趋势

### 6.4 分类支出占比图（饼图）

- 甜甜圈图展示当月各分类支出比例
- 右侧图例显示分类名称和金额
- 不同分类用不同颜色区分

### 6.5 交易明细列表

- 按日期分组显示当月所有交易
- 每组可展开查看详细交易
- 每条交易显示分类、备注、金额

---

## 七、交易记录

点击首页快捷操作区的 **"交易记录"** 进入。

- 展示所有历史交易记录（不分月）
- 按日期倒序排列
- 每天一组，显示当日收支合计
- 点击可展开查看当天每笔交易详情

---

## 八、分类说明

### 支出分类

| 一级分类 | 二级分类 |
|----------|---------|
| 餐饮 | 早餐、午餐、晚餐、零食/夜宵 |
| 交通 | 公交/地铁、打车、加油/停车 |
| 生活 | 房租/房贷、水电燃气、通讯费、日用品 |
| 饮料酒水 | 奶茶/咖啡、饮料、酒水 |
| 比赛/活动 | 报名费、装备费、差旅费 |
| 娱乐 | 电影/演出、游戏、运动健身 |
| 购物 | 衣物、电子产品、其他购物 |
| 其他 | 医疗、教育、社交/礼物 |

### 收入分类

| 一级分类 | 二级分类 |
|----------|---------|
| 工资 | 基本工资、奖金、加班费 |
| 投资理财 | 利息、股息、投资收益 |
| 其他收入 | 红包、退款、兼职 |

---

## 九、常见问题

**Q: 为什么收入记账不需要选分类？**
A: 收入记账已简化为只需选择账户和输入金额，无需分类。

**Q: 预算设置中主分类没有输入框？**
A: 是的。主分类预算 = 子分类预算之和，自动计算。您只需在子分类中输入预算即可。

**Q: 数据存在哪里？会丢失吗？**
A: 所有数据存储在设备本地。Android 路径：`/Android/data/<包名>/files/jizhang_data/data.json`。只要不卸载应用或清除存储，数据不会丢失。

**Q: 可以备份数据吗？**
A: 您可以手动复制 `data.json` 文件进行备份。

**Q: 预算按什么周期？**
A: 预算按月设置，每月独立。下月需重新设置预算。

**Q: 支持几个账户？**
A: 默认两个（微信、支付宝），可在账户设置中无限添加。

**Q: 记账后余额不对？**
A: 支出会减少账户余额，收入会增加账户余额。可在账户设置中查看和调整。

**Q: 为什么记账页面看不到某些分类？**
A: 支出分类只显示已设置预算的分类。请先到"预算管理"中为子分类设置预算。

**Q: 如何添加自定义分类？**
A: 进入预算管理页面，点击右上角的 **+** 按钮，输入主分类名称、选择图标、填写子分类数量和名称即可。
